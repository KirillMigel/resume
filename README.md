# Resumio — Конструктор резюме

Интерактивный сервис для создания профессиональных резюме с живым предпросмотром и экспортом в PDF.

## Возможности

- **Аутентификация**: Регистрация и вход через Supabase Auth
- **Управление резюме**: Создание, редактирование, дублирование и удаление резюме
- **Мгновенный предпросмотр**: Любой ввод сразу отображается в правой колонке
- **Автосохранение**: Данные автоматически сохраняются в Supabase при редактировании
- **Экспорт PDF**: Скачивание готового резюме в формате PDF с корректной версткой
- **Полная функциональность**: 
  - Личные данные с загрузкой фото
  - Опыт работы (неограниченное количество записей)
  - Образование
  - Навыки
  - Ссылки (LinkedIn, портфолио и т.д.)

## Локальный запуск

1. Установите зависимости:
```bash
npm install
```

2. Настройте переменные окружения:
Создайте файл `.env.local` в корне проекта:
```
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

3. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно на http://localhost:3000

## Как пользоваться

1. **Регистрация/Вход**: Создайте аккаунт или войдите в существующий
2. **Создание резюме**: Нажмите "Создать резюме" на главной странице
3. **Заполнение данных**: 
   - Личные данные (имя, должность, контакты, фото)
   - Опыт работы (добавляйте несколько мест работы)
   - Образование
   - Навыки и ссылки
4. **Предпросмотр**: Смотрите живой предпросмотр справа (на десктопе)
5. **Сохранение**: Все изменения автоматически сохраняются
6. **Экспорт**: Нажмите "Скачать" для получения PDF-файла

## Стек

- **Next.js 14** (App Router, TypeScript)
- **React 18** с хуками и контекстом
- **Tailwind CSS v4** для стилизации
- **Supabase** для аутентификации и хранения данных
- **PDFKit** для серверной генерации PDF
- **TypeScript** для типобезопасности

## Структура проекта

```
src/
  app/
    api/export/route.ts    # API для экспорта PDF
    auth/page.tsx          # Страница аутентификации
    dashboard/page.tsx      # Дашборд со списком резюме
    builder/page.tsx        # Страница редактора резюме
    layout.tsx             # Корневой layout
    page.tsx               # Главная страница
  components/
    auth/                  # Провайдер аутентификации
    resume/                # Компоненты резюме
      resume-builder.tsx   # Главный компонент редактора
      resume-form.tsx      # Форма редактирования
      resume-preview.tsx   # Предпросмотр резюме
      resume-provider.tsx  # Провайдер состояния резюме
      export-button.tsx    # Кнопка экспорта
  lib/
    pdf.ts                 # Генератор PDF
    resume-data.ts         # Типы и данные резюме
    supabase/              # Клиент и функции Supabase
```

## База данных

Проект использует Supabase. Необходимо создать таблицу `resumes`:

```sql
CREATE TABLE resumes (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  title TEXT NOT NULL,
  data JSONB NOT NULL,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_resumes_user_id ON resumes(user_id);
CREATE INDEX idx_resumes_updated_at ON resumes(updated_at DESC);
```

## Развертывание

Проект готов к развертыванию на Vercel или другой платформе, поддерживающей Next.js.

Убедитесь, что переменные окружения настроены в настройках проекта.
